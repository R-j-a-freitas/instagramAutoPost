# ============================================================
# Instagram Auto Post – Configuração
# Copia para .env e preenche os valores.
# Para OAuth (recomendado): só precisas de client_id/secret; o resto vem automaticamente.
# ============================================================

# --- Google Sheets ---
IG_SHEET_ID=1UBdukuHNvpfdcyBxKIQAt5pRIKFrGLYI6tZdYhfYCig

# OAuth (recomendado): "Ligar com Google" na app
# Cria em: Google Cloud Console → Credenciais → OAuth 2.0 Client ID (Web)
# URI de redirecionamento: http://localhost:8501/
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Fallback: Service Account (upload manual do JSON na app)
# GOOGLE_SERVICE_ACCOUNT_JSON=/caminho/para/service_account.json

# --- Instagram Graph API ---
# OAuth (recomendado): "Ligar com Instagram" na app
# Cria em: developers.facebook.com → Apps → Adicionar produto Instagram
# OAuth redirect URI: http://localhost:8501/
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=

# Fallback: credenciais manuais (token long-lived)
# IG_BUSINESS_ID=...
# IG_ACCESS_TOKEN=...

# --- Gemini (geração de imagens) ---
# Obtém em: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Cloudinary (upload de imagens) ---
# Opção 1: URL única
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Opção 2: variáveis separadas
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- OAuth redirect (IMPORTANTE se a app correr noutra porta) ---
# Se a app correr em 8502 (ex.: 8501 ocupada), define:
# OAUTH_REDIRECT_BASE=http://localhost:8502
# A URI no Google Cloud deve coincidir exatamente (ex.: http://localhost:8502/)

# --- Ambiente ---
ENV=dev
